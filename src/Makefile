
all:
	clang-10 -Wall -O3 -g -pg -mtune=native -march=native -ffast-math -funroll-loops -fopenmp op.c

#amd:
#	gcc -Wall -g -pg -O3 -mtune=znver2 -march=znver2 -ffast-math -funroll-loops  -fopenmp op.c

clean:
	rm -f a.out
